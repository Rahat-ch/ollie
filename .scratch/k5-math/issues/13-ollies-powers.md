# 13: Ollie's Powers

**What to build:** When the Learner Masters counting on, make-a-ten, unknown addend, or Unit 3, the Session's headline celebration shows Ollie learning a Power: Count-On Flight, Make-Ten Magic, Missing Number Detective, Story Solver. From then on, every matching Problem shows Ollie using it: wing beats along the number line, filling and splitting on the ten-frame, a magnifying-glass search for the missing part, richer Story variants. Powers appear on the Path, in the Parent Area, and by name in the Parent Summary. Powers are never lost and never bought.

**Blocked by:** 08 Play a Session, 10 Unit 3 Stories

**Status:** resolved

- [x] Mastery of the named Skill or Unit awards its Power exactly once and it persists
- [x] Each Power has its own animation on matching Problems, built to the character sheet poses
- [x] The Power celebration replaces the ordinary Session celebration on the Session it is earned
- [x] Story Solver switches Unit 3 to the richer Story variant set
- [x] Path, Parent Area, and Parent Summary all show Powers by name; Badges do not exist anywhere

## Comments

**2026-09-13, implementation.** The Powers are behind the Loop seam, as the spec's "Two seams" note puts them. `src/loop/powers.ts` is the catalogue and the whole of the rule: four `Power` rows in the order the progression teaches them (id, name, what must be Mastered, the Skills Ollie uses it on, and what it does in the Learner's words) — Count-On Flight from counting on, Make-Ten Magic from make-a-ten, Missing Number Detective from unknown addend, Story Solver from Unit 3, and a fifth is a spec change. `powersFor(profile)` reads the Powers a Profile's Mastery has taught (a Unit's Power waits for every Skill in that Unit), `powersEarned(before, after)` is what a Session taught, `withPowers(held, earned)` is the list afterwards, and `powerUsedOn(held, problem)` is the Power Ollie uses on a Problem. `SessionResult` gained `powersEarned` and `finishSession` fills it from the Profile before the Session and the one after, so a Power is a pure function of Mastery: earned on exactly the Session that Masters its Skill or Unit, never bought, never deducted, and never re-awarded (Mastery is latched in the Loop, and the Profile's list is a union besides). The Loop tests drive it from outside with the Simulated Learners' own answer policy: the strong Learner playing twenty counting-on Sessions teaches Ollie Count-On Flight exactly once, the weak Learner teaches it no more than once and only if the Skill is Mastered, a Session of Reveals on an already Mastered Skill earns nothing, and Story Solver arrives on the Session that Masters the second Unit 3 Skill and not the first.

The Profile keeps them: `Profile.powers`, `PROFILE_VERSION` 5, and a version-gated migration like tickets 10 and 14 — a Profile stored at version 1 to 4 is carried forward with the Powers its own Mastery has already taught (`powersFor(progress)`), so a Learner who Mastered counting on before Ollie could learn from it finds Count-On Flight waiting, and a `powers` field that is not four ids this version knows starts the Profile fresh. `beginPlay` reads the Powers onto the Play state, `celebrate` adds what the Session earned, and the Session screen writes them with the progress and the rewards when it celebrates, so celebrating the same Session twice adds nothing.

On screen: `visualFor(problem, stage, powers)` now carries the `power` the visual is drawn with (the models are otherwise byte-for-byte what they were: a Power changes how a visual is drawn, never what it says), and the Session screen puts the Power's pose on Ollie the moment a matching Problem is asked, keeping the phase's own pose where Ollie is reacting (encourage after a miss, celebrate on a right answer) and the beak on its own class, so a Power pose still speaks (ticket 11 untouched). Each Power's rig animation is in `src/ollie/ollie.css` to the character sheet's hinges: both wings beat and Ollie rises for Count-On Flight, the wing carrying the ten-frame chip lifts and the plum sparkles pulse for Make-Ten Magic, the magnifying glass searches for Missing Number Detective, the book opens for Story Solver. The visuals' own moves are in `globals.css`: sky wing beats over each hop on the number line (`src/ui/NumberLine.tsx`), a magnifying glass built from the same shapes as the pose's prop sweeping from the number known up to the whole, and flat plum rings turning out of the counters that fill the ten and the ones left over (`.ten-frame-magic`). Everything is bounded — three beats, two searches, one page turn; nothing loops — and every one of them is in the `prefers-reduced-motion` block. `src/ui/PowerMark.tsx` is each Power's mark on a plum disc (wing, ten-frame chip, glass, book), used on the Path stop, in the Parent Area, and on the celebration card.

The celebration: when a Session earns a Power the headline is the Power ("You taught me Count-On Flight!", `powerLine` in `src/play/lines.ts`, added to `ollieLines()` so `pnpm voice:lines` renders all four — Ollie's catalogue is now 124 lines and 3,191 characters), Ollie takes the Power's pose, and a plum card names it with what Ollie will do. Ticket 14's Coins and Streak cards are still there and the ten Coins are still paid; the Mastered card is still there too. Two Powers in one Session is possible (make-a-ten and unknown addend can both be Mastered at once): the first is the headline and both get a card. Story Solver switches Unit 3 to the rich Story set: `StoryInput.set` (`"plain"` or `"rich"`) threads through the prompt (one more line of brief: say where it happens and what the things are doing), `poolKey` (a `rich/` prefix, so the plain keys are untouched), `templateStory` (a second hand-written sentence per structure in `src/story/shapes.ts`, using a new `where` phrase per Theme built from words the Theme already allows), the `/api/story` route, `pnpm pool --rich`, and `useStories`, which asks for the rich set when the Learner has taught Story Solver. The validator is unchanged, so a rich Story is still two sentences under 25 words with exactly the engine's numbers, and a test checks the rich template is valid for every Theme and structure — the template path is what this machine shows, the bundled Pool still being empty for want of a key.

Readings to confirm, recorded as amendments 28 to 30 in `decisions.md`: "richer Story variants" is read as the rich set above (more Theme, not more text, because a Story must still be holdable after one hearing); a Power is worn from the moment a matching Problem is asked, while what it draws follows the marks that stage already shows (the magnifying glass searches the gap an unknown-addend Problem shows from the start; the wing beats and the rings ride on the hops and counters that appear with the Hint and the Reveal, because drawing the hops while the Problem is being asked would give the answer away); and the Powers held are stored on the Profile rather than derived on every read, so "never lost" is a property of the data and not of the Mastery rule. Also worth a look: Story Solver's rich set degrades to the same Story as the plain set only if `pnpm pool` is run without `--rich` (the route then writes one live, or falls back to the rich template); and the Parent Area lists all four Powers with the ones not yet learned dimmed and their Mastery named, which the ticket does not ask for but reads better than an empty list.

Deferred. The Parent Summary's Powers list waited on ticket 12 and is now wired (see the note below). Placeholders for ticket 15: the four Power marks and the celebration card are flat token-palette SVG in the right shapes but not hand-tuned, the wing beats on the number line are two plain sky arcs, and the magnifying glass is the pose's prop redrawn at 22px; each Power's pose animation exists and is visibly distinct but the poses themselves are ticket 02's placeholders.

Vocabulary gaps for domain modelling: "pose" and "prop" (the rig's, from ticket 02), "the rich Story set" and "where" (a Theme's place phrase), "mark" (a Power's little picture, already used for the counters' Hint marks), "wing beat", and "held" (the Powers a Profile has, as against the Powers that exist) are used in code and here and are not in the glossary.

Tests: `src/loop/powers.test.ts` (the catalogue in order against the spec's names, what each Power is taught by, `powersFor` over a fresh and a Mastered Profile, the award once and once only through `runSession`, a Session of Reveals on a Mastered Skill, Story Solver on the second Unit 3 Skill, twenty Sessions of two Simulated Learners, `withPowers`, and `powerUsedOn`), `src/parent/powers.test.ts` (all four by name with what teaches them and what Ollie does, learned and not), `src/play/path.test.ts` (each Power at the Unit that taught it, and only once learned), `src/play/visuals.test.ts` (the Power each visual is drawn with, none until it is taught, and that nothing else about the model changes), `src/play/play.test.ts` (the Powers carried through a Session, earned on the Session that Masters counting on, and not earned twice), `src/profile/profile.test.ts` (a fresh Profile has none, the round trip, the version 4 migration deriving them from Mastery, and a `powers` field this version does not know), `src/story/pool.test.ts` and `template.test.ts` and `prompt.test.ts` and `src/play/stories.test.ts` and `src/app/api/story/route.test.ts` (the rich set keyed apart, read apart, filled apart, valid for every Theme and structure, and asked for by the route), `src/voice/lines.test.ts` (the four Power lines in Ollie's catalogue), and `e2e/powers.spec.ts`: a Profile one Session away from Mastering counting on (seeded at version 4, so the browser runs the migration too) plays that Session with no Power on any Problem, sees the Count-On Flight celebration with Ollie in its pose and the Coins and Streak beside it, finds the Power on the Path, plays on until counting on comes back as a Review Problem and sees Ollie flying it with one pair of wing beats per hop while the make-a-ten Problems beside it have none, and after a reload finds the Power on the Path and the four Powers by name in the Parent Area with only Count-On Flight learned; no request leaves the host. Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (491), `pnpm build`, and `pnpm test:e2e` (26).

**2026-09-13, after merging ticket 12.** Main had moved: the Coach in the app, the Parent Summary, and Ollie's Notebook were merged, and with them a Profile at version 5 carrying the Coach's record. The Profile is now **version 6** with both the record and the Powers, and the migration handles each stored version in turn: version 5 keeps the record and gains the Powers its Mastery has already taught, version 4 and below gain both, and a `powers` field that is not four ids this version knows still starts the Profile fresh. A Session waiting for its Coach run is kept on the record, so the Powers it earned are kept with it: `SessionResultSchema` gained `powersEarned`, defaulting to none so a record written before Powers existed still parses, and a test covers each of those paths. This ticket's readings are now amendments **28 to 30** in `decisions.md`, ticket 12's having taken 25 to 27.

The Parent Summary names the Powers. Ticket 12 built the whole thread — `SummaryInput.powers`, the prompt's "Powers earned this Session", the template's "Ollie learned …", and the stored `ParentSummary.powers` — and left the one call site passing an empty list. `powersEarnedIn(result)` in `src/play/coaching.ts` is that list: the Powers the Session earned, by name, read from the Session's own result, which is what the record holds while it waits, so a reload mid-run names the same Powers rather than reaching for a screen's state. `use-coach.ts` passes it. `src/play/coaching.test.ts` drives the Summary seam with a Session that Masters counting on: the Loop returns Count-On Flight, the Summary written on the fake names it in its prose and keeps it in `powers`, and with nothing to write it the hand-written template says "Ollie learned Count-On Flight." too.

**2026-09-13, after code review.** The reduced-motion claim was false: merging ticket 12 took the Powers' classes back out of the `prefers-reduced-motion` block in `globals.css` without a conflict, so `.hop-beat`, `.detective-glass`, the two `.ten-frame-magic` counters, and `.story-page` were animating for a Learner who asked for less motion. They are back, and `src/app/globals.test.ts` now reads the stylesheet and fails unless every selector that sets an animation is either in that block or in a one-entry `KEPT` list — the Parent Gate's ring, which is how much of the hold is left and not decoration. A Power added with a move and no reduced-motion rule fails there too. Vocabulary: "ability", which the glossary's Power entry avoids, is gone from `src/loop/powers.ts` and `src/loop/types.ts`, and the README no longer names Badges at all, since the app does not have them; the README's Profile version is 6. Amendment 29 said the visual's move waits for the Hint, which is only true of two of the three: the magnifying glass sweeps between the two numbers an unknown-addend Problem shows from the start, so it searches while the Problem is being asked and gives nothing away, while the wing beats and the plum rings ride on the hops and counters that appear with the Hint and the Reveal. The amendment, the README, and the note above now say that.

Two tests asserted what the code says rather than what it should do. `src/parent/powers.test.ts` no longer repeats a `does` string: it asserts the copy's properties (one sentence each, all different, each beginning "Ollie" and naming what it does it with) and that every Power in the catalogue is listed in order. `src/loop/powers.test.ts` no longer compares Mastery to `earned.length > 0`: the weak Learner is played for seven Sessions of make-a-ten (nothing Mastered, nothing earned) and then for twenty (Mastered, with Make-Ten Magic earned on the eighth), and the strong Learner's Count-On Flight is pinned to the Session it arrives on.

Smells: `Power` now carries its `unit`, so the Path stop and the Parent Area read it instead of each unpicking `mastery.kind`, and a test checks it against the Unit of the Skill or Unit that teaches it, so the two cannot drift. The `rich` boolean is a named `StorySet` (`"plain" | "rich"`) on `StoryInput` and so on the Pool input, the route's body, and `poolInputs`; `pnpm pool --rich` is still the flag. `src/ollie/powers.ts` maps each Power to its pose explicitly rather than leaning on the ids matching the pose files, with `poses.test.ts` checking every pose in the map is one the rig has and that no two Powers share a drawing. The unused `PowerChip` is gone, `isOne` is `isOrAre`, the `arrive` comment says what it renders, `withPowers` is applied once (the Play reducer's, which the screen writes), and `public/ollie/README.md` documents the three prop origins the Power animations turn about — the ten-frame chip at (30, 47), the glass at (208, 34), the book's spine at (120, 167) — which are now the props' own middles rather than the two guesses that were there. Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (568), `pnpm build`, and `pnpm test:e2e` (34).

# 15: Illustration pass in the design language

**What to build:** Every placeholder is replaced with final art in the approved style: Ollie in all states and Power poses, six Theme illustrations, four Avatar bases and six items, the celebration and milestone visuals. All SVG-first and hand-tuned to the character sheet so nothing reads as machine-generated. Image-model output, if any, was used for reference only; anything generated that ships is listed in THIRD_PARTY.

**Blocked by:** 02 Design direction, 13 Ollie's Powers, 14 Streak, Coins, and Shop

**Status:** resolved

- [x] No placeholder art remains anywhere in the Learner or Parent flows
- [x] All art matches the character sheet and tokens; a side-by-side review against the canvas is recorded as approved by the user
- [x] Assets are SVG where possible and total under a stated size budget so first load stays fast on a tablet
- [x] THIRD_PARTY lists every asset source and any generative-AI involvement

## Comments

**2026-09-13, implementation.** Every placeholder named in the Comments of tickets 08, 09, 13, and 14 is now final art, hand-written as SVG source in the style paragraph of `docs/design/direction.md`: filled paths, one deep tone of the same hue offset behind as the paper shadow, the token palette only, and the one line the style allows (3px ink at 60%, `INK_STROKE` in `src/ui/icons.tsx`) where a drawing needs it. No image model was used for anything in this repo and nothing was downloaded; `THIRD_PARTY.md` now lists each group of assets by file with its provenance and says so plainly.

Ollie (`public/ollie/*.svg`, regenerated into `src/ollie/poses.generated.ts` by `pnpm ollie:poses`) was tuned against the character sheet, not redesigned: the wing keeps its hinge at (56, 122)/(184, 122) but now ends in three cut feather tips, so a raised wing reads as a wing and not a paddle; celebrate's rotation drops from 168° to 160° so the ear tufts stay visible over it; the open beak is one shape family at three depths (mouth in rust-deep, lower mandible in sun-deep, upper mandible in sun; small, mid, wide) instead of the drooping two-piece shape it had, so a talking, cheering, or encouraging Ollie all read as the same mouth; the brows are one shape translated per pose, listed in `public/ollie/README.md`; Count-On Flight's wing beats are three sky arcs sweeping under each raised wing rather than six crescents floating beside the shoulders; Make-Ten Magic's prop is a ten-frame filled to ten, moved fully inside the 240 box (it was clipped at the left edge) with three plum sparkles, still turning about (30, 47) so `src/ollie/ollie.css` is untouched; the detective's glass gained its flat catchlight; and `ollie-head.svg` — which the favicon set and the Path's current stop use — was rescaled from the pose geometry by 112/155 about the eyes, because its beak was about half again too big for the face and read as a different bird. No coordinate carries more than one decimal place now, and there is no editor cruft. `pnpm brand:images` was rerun, so `icon.svg`, `favicon.ico`, `apple-icon.png`, and `opengraph-image.png` are the new drawing.

The rest of the art is inline SVG in the component that owns it, which is where the placeholders were. `src/ui/ThemeIcon.tsx` holds six new pictures in one 96×96 frame at one silhouette weight — a puppy with a bone, a plated dinosaur, a rocket with a flame and three paper stars, a striped fish over an ink wave, a fairy with a wand, a tipper truck with a load — drawn so the same file reads at 176px in the Story card and at 44–48px in onboarding and the Shop's Theme picker, so there is no second small variant to keep in step. `src/ui/Avatar.tsx` is a round eared creature with the style's own eye stack (white, teal iris, ink pupil, one flat catchlight), a sun beak, a smile, and two feet, deliberately unlike Ollie, who is tall and tufted; `src/ui/AvatarItems.tsx` draws the six Items against the Avatar's landmarks (the crown of the head at y 20, the eyes at y 41, the neck at y 58, the ground to the right), so one drawing sits correctly on all four colour bases — the review page shows all twenty-four combinations. `src/ui/PowerMark.tsx` is four paper shapes cut out of a plum disc: three hops on a number line, a ten-frame filled to ten, the glass, the open book. `src/ui/RewardChips.tsx` has a real flame for the Streak instead of the plain paper droplet, and the Coin gained a deep-tone rim, because on the sun Coin chip a sun disc had all but disappeared. In `src/app/play/Celebration.tsx` the confetti is now three cut shapes rather than two, and the milestone mark is a ten-petal sun rosette round a leaf disc with the Streak's own flame in it, so a milestone belongs to the Streak rather than looking like a second reward; `.milestone` and `.milestone-rays` are unchanged and `src/app/globals.test.ts` still passes, so everything animated is still in the reduced-motion block. `data-`attributes and every component prop are unchanged; `data-theme` on the Theme picture and `data-item` on the Shop's Item tile were added, nothing was removed.

**The size budget, stated and measured.** All hand-authored SVG art under 120 KB uncompressed, and the Home screen's whole first load under 200 KB over the wire. Measured: the nine pose files are 29,735 bytes and the seven inline art modules (`Avatar`, `AvatarItems`, `ThemeIcon`, `PowerMark`, `RewardChips`, `LockIcon`, `icons`) are 23,356 bytes of source including their comments, so 53,091 bytes, 44% of the budget; `poses.generated.ts` is the same SVG again as a module (30,385 bytes) and is not counted twice. The Home screen with a Profile is 27 requests and 61,627 bytes encoded, of which the only SVG file fetched is `ollie-head.svg` at 616 bytes encoded (1,462 raw) and the rest of the art rides inside the route's JS; the three self-hosted woff2 add 55,252 bytes on a cold cache, so 116,879 bytes in all, 58% of the budget. The brand rasters are `favicon.ico` 2,476, `apple-icon.png` 8,699, and `opengraph-image.png` 84,607 bytes; only the favicon is on a Learner's first load. `docs/design/screens/` is 1.7 MB of review screenshots and never enters the image (`docs` is in `.dockerignore`).

**The review.** `docs/design/review-2026-09-13.html` is a single static page with no external resources: the app's own compiled CSS inlined, the fonts and Ollie's head referenced by relative path into the repo, and every asset the app's own markup. It shows Ollie's nine drawings each beside what the character sheet asks of that pose, the six Themes at both sizes, the four bases, each Item on its own and worn on all four bases, the four Power marks, the Coin and the Streak, the four Power cards and the 3, 7, and 14 milestone cards, and then eleven Playwright screenshots of the real screens at the 1024×768 tablet frame (onboarding's three steps, Home with the Path, a Session on the ten-frame, on the number line, on a Hint, and on a Unit 3 Story, the celebration, the Parent Area with a Summary and a Power, and the Shop). It is written by `pnpm design:review` from the running app, against `/design-review`, a route that is in the app but linked from nowhere, so the review can never drift from what ships; `Milestone`, `PowerCard`, and `Confetti` are exported from `Celebration.tsx` for it. Every screenshot was looked at and the art was corrected from them — the dinosaur, the fish, the puppy's bone, the Power marks, the Coin, the Avatar's feet and the bunny's tail all changed after a look.

**The approval box is the user's.** The page awaits review: open `docs/design/review-2026-09-13.html` in a browser, or run the app and open `/design-review`. Three judgement calls to confirm. First, the Path's current stop puts the rust head on a sun disc, which is the canvas's own treatment, and Ollie's amber beak is close in value to that sun; it was left as the canvas has it rather than changed unasked, but a paper disc behind the head would make him read — say the word. Second, `/design-review` ships as an unlinked static route; it can be deleted before the submission if you would rather the app had only the four screens, at the cost of `pnpm design:review` needing it back. Third, the Avatar is drawn to be plainly not Ollie (round, two ears, no tufts, a smile rather than a beak-and-belly); if you want it closer to him, it is one file.

Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (568 in 62 files), `pnpm build`, `pnpm test:e2e` (34), and `pnpm licenses:check`, all passing with no assertion weakened.

**2026-09-13, after code review.** Seven visual defects and six standards points fixed. `/design-review` is now a server page that calls `notFound()` unless `DESIGN_REVIEW=1` (`src/app/design-review/page.tsx`, `dynamic = "force-dynamic"`, the harness itself moved to `DesignReview.tsx`), so it is 404 in production; `pnpm design:review` starts its own server with the variable set, drives it, and stops it, and its date is today's rather than a literal, so the page carries no date of its own — the file name has it. The confetti moved behind the celebration's content (`isolate` on the stage, `-z-10` on the confetti svg); confirmed in fresh clips of the title with reduced motion both on and off, the paper now passing behind the letters in both. The Pet Snail moved down into the bottom-right corner with short cut eyestalks, so nothing of it lands on the Avatar's face on any of the four bases. The Path's current stop puts Ollie's head on a paper badge inside the sun disc, the sun ring kept, the head down to 64px so the badge holds it whole. The Count-On Flight mark is two hops on a number line with a pair of wing beats flying over them, the same shape `NumberLine` draws. Onboarding's second step renders each choice as the Avatar itself in that colour rather than a plain circle; the button's behaviour, `aria-pressed`, and the preview's `data-color` are unchanged.

Standards: the glasses rims, bridge and arms, the snail's eyestalks, and its shell (now flat concentric rings rather than a drawn spiral) are filled paths, and the Power mark's book lines are filled bars, so the only strokes left anywhere are 3px ink at 60% through `INK_STROKE` and the two the style paragraph names on Ollie's glass and book. The eye stack and the four-pointed star are one shared cut each in the new `src/ui/art.tsx`, used by the Avatar and by the puppy, the dinosaur, the fish, the rocket's stars and the fairy's wand. The Avatar's comment no longer says "character", the milestone's no longer says "reward", and the nine SVGs and `Ollie.tsx` now label him "Ollie, idle" and so on rather than "Ollie the owl"; `e2e/home.spec.ts` matches `/^Ollie, /`, which is not a weaker assertion. The review harness types its captions `Readonly<Record<ThemeId, string>>` and builds what the Avatar wears with a typed helper, so the `as never` is gone, and its Power-mark note says what the code draws. THIRD_PARTY lost the dangling sentence about items marked planned and now names `src/ui/art.tsx` beside the rest.

**The numbers, remeasured.** The first-load figure in the paragraph above was read on a warm cache and was wrong. `pnpm design:review` now ends by printing a cold load of the Home screen, taken in a context whose `storageState` carries the Profile so nothing is fetched before the measured load: **27 requests, 399,503 bytes encoded**. Of that, 56,152 bytes are the three self-hosted woff2 and about 268,000 bytes are the React and Next runtime chunks every route shares; the only SVG file fetched is `ollie-head.svg` at 616 bytes encoded, and the rest of the art rides inside the route's JS. The art budget is unchanged and still the one the ticket asks for: all hand-authored SVG under 120 KB uncompressed, measured at **73,842 bytes** (the nine pose files 29,663, `src/app/icon.svg` 1,454, and the twelve inline art modules 42,725 — `Avatar`, `AvatarItems`, `ThemeIcon`, `PowerMark`, `RewardChips`, `LockIcon`, `icons`, `art`, `Path`, `NumberLine`, `TenFrame`, and `Celebration`, the set THIRD_PARTY discloses), 62% of it. The first-load budget is restated honestly against what a cold tablet actually fetches: under 450 KB encoded, of which the art is under 25 KB; 399,503 bytes measured.

Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (568 in 62 files), `pnpm build`, `pnpm test:e2e` (34), `pnpm licenses:check`, and `pnpm design:review`; `/design-review` answers 404 on the production build without the variable and 200 with it, while `/` answers 200 either way.

**2026-09-18, approved.** The user reviewed the art on the deployed iPad build and the review page and signed it off ("design is good consider that signed off on"); the ticket is resolved.

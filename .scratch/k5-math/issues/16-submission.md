# 16: Submission package

**What to build:** The entry is complete: ollie.rahatcodes.com runs the final build; the README maps every Skill to its CCSS standard and states the curriculum claim precisely; the write-up describes what it does, how it was built, and next steps, and states exactly what is generative and what is not; THIRD_PARTY is final and pasted into the form; the latest eval report is cited with its headline numbers; and a 2 to 3 minute screen-recorded video with the entrant as the Learner follows the one-story plan: problem, play with a crossing-ten miss, the Notebook, one architecture graphic, the convergence chart, the tagline. No identifiable person other than the entrant appears.

**Blocked by:** 07 Coach evals, 11 Ollie's voice, 12 Coach in the app, 15 Illustration pass

**Status:** ready-for-human — the video, the form and its confirmation, and every run that needs a vendor key are the user's; everything else is done and verified.

- [x] README contains the Skill-to-standard table and the sentence 'A focused Grade 1 arithmetic progression aligned to key CCSS 1.OA and 1.NBT concepts'
- [x] The write-up contains the two taglines and the exact generative/deterministic split
- [ ] The video is under three minutes, shows only the entrant, and follows the storyline in the spec
- [x] The eval report cited in the write-up is the latest committed report and its chart matches
- [x] THIRD_PARTY discloses every third-party API, model, font, image, audio source, OSS licence, and generative-AI assistance
- [ ] The submission form is filled and the confirmation is saved

## Comments

**2026-09-13, implementation.** Four boxes are ticked and two are the user's, which is why this is ready-for-human: the video has to be recorded by the entrant and the form submitted by the entrant, and both want an `ANTHROPIC_API_KEY` on the host first. Everything written here is `docs/submission/`, four files.

`write-up.md` is the entry's written description, about 1,270 words in the first person: what Ollie does (read aloud, tap to answer, ten-frame and number line, Hint then Reveal with no penalty, the Coach after every Session, the Parent Gate and what is behind it, Powers, Coins and the Streak), the curriculum claim verbatim with a pointer to the README's table and the research folder cited, then how it was built — the two seams (the Loop as a pure function, Generation as one interface with four operations and a fake), the bounded Plan Space, the evidence check that rejects a Hypothesis citing a Problem the Coach was never shown, one retry and then the Baseline Plan with the Notebook saying so, the Content Pool with its deterministic Story validator, the Speech Chain, the Profile in localStorage — then the evals against the Baseline over the six Simulated Learners. The generative/deterministic split is stated as the ticket asks: generative are the Stories in Theme and Nickname, the Coach's Hypotheses and Plans, the Parent Summary's words, and Ollie's voice audio; deterministic are the arithmetic, the answers, the Hints, Plan validation, the evidence checks, Mastery, Powers, Coins, and the Streak. Both lists are wider than the spec's own phrasing, which sets only Stories and the Coach's Hypotheses and Plans against the arithmetic; they extend it to what tickets 11 and 12 built — the voice audio, and the Parent Summary's words over the engine's tally — which is the entry as it actually stands. Both taglines are in it, the first as the standfirst and the second as the last line, and the architecture line rides on the graphic. It cites `docs/evals/2026-09-13T17-38-41Z.json` by path with its chart, says in bold that the report is from `pnpm eval --fake` and what that means, and reads its numbers off the JSON: Evidence Integrity 1.00 over 2,331 citations (1,502 tuning, 829 held out, zero unknown IDs, zero inconsistent); detection 0 of 2 planted weaknesses, which is what the fake is built to score; false positives 3 of 18 supported Hypotheses on the tuning split and 0 of 8 held out; 120 of 120 Plans taken from the Coach with no retry and no Baseline fallback; mean Skills Mastered by Session 20 of 6.5 Coach against 6.0 Baseline on the tuning split and 5.0 against 6.0 held out, 36 against 36 over all six Learners, with the sentence that the fake does not beat the Baseline and no claim that it does; Story validity 30 of 30 and Parent Summary validity 6 of 6 on the first attempt with no template fallbacks; and the Judge withheld at 12 of 20 Stories and 6 of 10 Summaries against the 0.8 threshold, with the same-family limitation stated as `decisions.md` amendment 10 requires. A "What has not run live" section names the four model operations and the voice as never called.

`architecture.svg` is the one graphic, hand-authored to `docs/design/direction.md`: paper ground, flat paper shadows as an offset deep-tone shape with no blur, 20px radii, the token palette only, and the chart's own font stack so it needs no font file. Six boxes as a closed loop — Session → engine → evidence → Coach → validator → next Session — with the engine's three in leaf, the Coach in plum (the direction's colour for the Notebook's Hypotheses), the two the Learner plays in sun, a legend saying which is deterministic and which generative, and "The AI can personalise the learning path. It cannot make up the math." across the bottom. It is rendered into the write-up with a Markdown image. One thing the spec asks for could not be honoured as written: it calls this "the nine-word line", but the only line it names for this graphic, in its own Taglines section, is thirteen words. The graphic and the video script carry that line verbatim and `video-plan.md` ends with a note putting the discrepancy to the user, beside the story 32 wording that ticket 11 left open.

`video-plan.md` is the shot list on the spec's timings (0:00 the problem, 0:15 play, 1:20 the Notebook, 2:00 the graphic, 2:25 the chart, 2:50 the tagline), with the exact taps and a checklist for the cut. The seeding recipe is a `localStorage` write to paste into the console on the live origin, and it is true rather than plausible: the Profile it writes was run through `parseProfile` and `beginPlay` here, and the eight Problems in the plan's table are what the Loop actually builds from it — p32 partners to 10 as a Review Problem (8 + ? = 10, answer 2, the easy non-crossing sum), p33 make-a-ten 7 + 8 (the crossing-ten miss, so the ten-frame Hint is the one that fills to ten), p34, p36 to p39 make-a-ten, and p35 counting on 15 + 3 as a Review Problem, which is where Ollie wears Count-On Flight because the seeded Profile has Mastered counting on. Every make-a-ten Problem crosses ten by construction, so the crossing-ten shot cannot miss. The Nickname in the recipe is the entrant's own, since the terms forbid depicting an identifiable person other than the entrant and a child's name on screen invites exactly that reading. The plan says plainly that the 1:20 Notebook shot needs a live Coach run and therefore a key on the host, that the Coach may name the Skill rather than the pattern and the narration should read what is actually on screen, and that seeding a Coach record to fake the shot is not an option.

`form.md` is the form field by field — title, one-line description, prompt, the live URL, the repo URL, the video link placeholder, the write-up, the THIRD_PARTY paste with a short substitute for the 373-row dependency table if the field is too small, and a notes-to-judges block — with the requirements taken from `docs/research/k5-math-game/05-nerdy-hackathon-alignment.md` and a pre-submit checklist.

The README gained a Submission section pointing at all four files, THIRD_PARTY, and the report with its chart, and a "What has and has not run live" paragraph that names every model operation and every voice call as never exercised, every committed report as `--fake`, and the Pool file as empty and `public/voice/` as not in the tree at all, against what has run. The Curriculum table was checked row by row against `src/loop/skills.ts` and is right: partners to 10 K.OA.4 (prerequisite) and 1.OA.6, teen numbers 1.NBT.2b, counting on 1.OA.5, make-a-ten 1.OA.6, unknown addend 1.OA.4, and both Unit 3 Skills 1.OA.1, with the Unit names matching `unitName`; the exact sentence is there. THIRD_PARTY needed no rewrite — it had no "planned" markers left, and ticket 11 and 15 had already resolved the voice and illustration entries — so the pass was `node scripts/third-party.mjs --write`, which moved only the audit date to 2026-09-13, and `pnpm licenses:check`: 373 packages, 370 permissive, the three MPL-2.0 build-time tools as reviewed exceptions, no GPL, LGPL, AGPL, or SSPL.

The live site was checked and not touched: `https://ollie.rahatcodes.com/` answers 200 through Cloudflare and `/api/health` answers `{"ok":true,"audioDir":"/data/audio","audioDirWritable":true}`, so the volume is mounted and writable. It is serving current main: all nine of `public/ollie/*.svg` are byte-identical to this worktree's, and those files last changed in `a96d47c`, the ticket 15 review commit at the head of main, so the illustration pass is deployed. `/design-review` answers 404, which is what it should do without `DESIGN_REVIEW=1`. Coolify deploys on merge; nothing was deployed from here. Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (568), `pnpm build`, and `pnpm licenses:check`.

**The closing checklist — everything that needs a key or the user.** In the order that unblocks the most:

1. **Set the Coolify environment variables** (`docs/deploy.md`): `ANTHROPIC_API_KEY` at minimum, then `ELEVENLABS_API_KEY` and `ELEVENLABS_VOICE_ID` once the voice exists. Runtime, not build-time; redeploy after.
2. **The first live Coach, Parent Summary, Story, and Judge runs**: `pnpm eval` with no `--fake`. Then commit the new report and `convergence.svg` and update the write-up's numbers and its `--fake` sentence — this is the spec's day-four checkpoint, still outstanding, and the one number in the entry that is currently a stand-in.
3. **Fill the Content Pool**: `pnpm pool` with the key. Ticket 10's estimate is about 2,800 Sonnet 5 calls (465 keys per Theme, 2,790 in all, one variant each); it resumes, and `--rich` fills the set the Story Solver Power opens.
4. **Ollie's voice**: `pnpm voice:design`, listen to the three previews, `pnpm voice:design --save <preview id>`, then `pnpm voice:lines` for the 124 fixed lines (3,191 characters) and `--kinds ollie,problem` as the budget allows. **Starter tier or above**, which is the cheapest tier carrying the commercial licence (THIRD_PARTY). Commit `public/voice/` with the manifest, and tick ticket 11's first box.
5. **Approve the ticket 15 review** at `docs/design/review-2026-09-13.html` and tick its second box.
5a. **Relabel the Calibration Set, or confirm it.** The 20 Story verdicts (ticket 10) and the 10 Parent Summary verdicts (ticket 12) in `src/evals/calibration.ts` are the implementer's, made on 2026-09-13, and they are the hand-made verdicts the Judge is gated against. Read them before the first live `pnpm eval`, since the Judge's readability and faithfulness scores appear only if it agrees with them on 80% or more; any verdict the user disagrees with should be changed before the run that the write-up will cite.
6. **Sign off the spec's user story 32 wording** (ticket 11, `decisions.md` amendment 24): the story still says the Nickname is "sent to generate Stories and audio", but Stories are written with a placeholder and only `/api/speech` carries the Nickname. And settle the "nine-word line" (this ticket, above).
7. **Record the video** to `docs/submission/video-plan.md`: seed the Profile with the console recipe, play the Session, under three minutes, entrant only, screen recording only. Then tick box 3 here.
8. **Fill the form** from `docs/submission/form.md`, paste THIRD_PARTY into the disclosure field, save the confirmation into `docs/submission/`, and tick box 6 here. Submissions close **2026-09-18, 11:59 PM CDT**.

**2026-09-13, after code review.** Nine findings, all taken. The second tagline was only inside `architecture.svg`, so a plain-text paste of the write-up into the form would have lost it: "The AI can personalise the learning path. It cannot make up the math." now opens "How it was built" as a sentence of its own and is the image's alt text too, with the first tagline still the standfirst. `public/voice/` was described as "committed empty" in the write-up and the README; it is not in the tree at all — `pnpm voice:lines` creates it — and both now say that. THIRD_PARTY was already right ("The bundled files are not in this commit"); ticket 11's dated implementation comment does say "`public/voice/` is empty", and that inaccuracy is left where it stands as part of that day's record and corrected here instead. The video plan's 1:20 cue now quotes the screen rather than paraphrasing it: the button reads "Show the 2 Problems this rests on" (the count being however many the Coach cited) and each row is the Problem ID, its equation from `formatEquation` (`7 + 8 = ?`), and `ASSISTANCE_WORDS` in full — "correct after a Hint · Session 5", Session 5 because the seeded Profile has four Sessions behind it. The write-up's "no child data" now carries ADR 0002's own clause: the Nickname is the one personal word that leaves the device and it leaves only so a line addressed to the Learner can be spoken, which is the speech route and nothing else; and a Story is now described as written with a placeholder the device fills, so it cannot be read as the Nickname being sent to write one. Vocabulary: "fallbacks" is out of the README's Speech Chain line (now the Content Pool's template sentence and every step of the chain down to the line on screen), "the curriculum defines/bounds" is "the engine bounds" in both the write-up and the video script, "whatever the score" is "for finishing it, not for getting it right", "AI Coach" survives only at its first mention where an outside reader needs the qualifier and is "the Coach" after, and the glossary's terms are capitalised through the form text and the video script — leaving the JSON keys in the seeding recipe alone, and leaving the taglines quoted verbatim. "What the evals say, honestly" is now "What the evals say". The generative and deterministic lists stand as they are, and the paragraph above now says plainly that they are wider than the spec's phrasing because they cover what tickets 11 and 12 built. `decisions.md` amendment 21 gained a dated note that `ollieLines()` is 124 lines and 3,191 characters since ticket 13's four Power lines, so the 120 and 3,066 in the amendment are not read as current. The closing checklist gained item 5a, the Calibration Set: the 20 Story and 10 Parent Summary verdicts are the implementer's and want the user's eye before the live `pnpm eval` the write-up will cite, since they are what gates the Judge. Re-verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (568 in 62 files), `pnpm build`, and `pnpm licenses:check` (373 packages, 370 permissive, 3 reviewed MPL-2.0 exceptions).

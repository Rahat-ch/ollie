# 02: Design direction canvas

**What to build:** Before any screen is built, the look is settled on a design canvas: palette, type, spacing, Ollie's character sheet (four base states and the four Power poses), a flat, warm, hand-tuned illustration style that does not read as AI-generated, and the core screens at tablet landscape: home with Path and Play, the Session screen with number pad and ten-frame, the celebration, onboarding, and the Parent Area with Ollie's Notebook. Produced with the design canvas skill so the user can adjust it visually; the UI tickets build to it.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] A design canvas exists with tokens (colours, type scale, spacing, radii) and the six core screens at tablet landscape
- [x] Ollie's character sheet shows idle, talking, celebrate, encourage, and one pose per Power in one consistent style
- [x] The illustration style is documented in one paragraph so later assets match it; image-model output is reference only
- [x] The user has reviewed the canvas and marked it approved
- [x] Tokens are exported in a form the app can consume

## Comments

**2026-09-10 (agent):** Canvas published for review at https://claude.ai/code/artifact/332d6f58-2ff2-4c31-b254-1332f2527629 (page "Direction": tokens, Ollie character sheet, Home, Session, Celebration, Onboarding, Parent Area, Ollie's Notebook; page "Alternates": a cool-night and a bold-poster sketch for comparison). Sources in `design/canvas/`, written direction in `docs/design/direction.md`, tokens in `src/app/tokens.css` and `src/design/tokens.ts` (kept in step by `src/design/tokens.test.ts`), Ollie's rig in `public/ollie/` with `README.md`. Fonts Fredoka and Andika self-hosted (SIL OFL). Added to this ticket at the user's request: a new favicon set (`src/app/icon.svg`, `favicon.ico`, `apple-icon.png`) and Open Graph image (`src/app/opengraph-image.png`), regenerated by `pnpm brand:images`. Waiting on the user to review the canvas and tick the approval box above; UI tickets build to this once approved.

**2026-09-10 (agent, after code review):** Review fixes applied: the Open Graph pitch now says Ollie reads aloud and the Coach informs the Parent (the Coach never speaks); the direction doc and touch-target tokens say Learner, not child; the onboarding caption no longer invites a first name; the Home artboard's Parent Gate reads "Hold for grown-ups" so it is not mistaken for the locked Unit; THIRD_PARTY.md discloses that the Ollie SVGs, canvas, and rendered icons were authored with Claude Code under the entrant's direction. Carried forward to the UI tickets: the Notebook mockup shows evidence as "8 + 5" chips with Assistance State; the real screen must also carry the Problem ID so the Parent can open it (spec: evidence is Problem IDs).

**2026-09-10 (user):** Approved the design direction on the canvas. Ticket closed; UI tickets build to `docs/design/direction.md` and `src/app/tokens.css`.

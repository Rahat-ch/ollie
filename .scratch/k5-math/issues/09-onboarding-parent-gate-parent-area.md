# 09: Onboarding, Parent Gate, and Parent Area

**What to build:** A Parent opens the app for the first time and sets up a Profile in under a minute: 'What should Ollie call you?' for the Nickname, an Avatar colour, a Theme from six illustrated choices, and a one-screen note that the Nickname is sent to generate Stories and audio and nothing else leaves the device. A press-and-hold Parent Gate opens the Parent Area, which shows Mastery per Skill and placeholders for Summaries, Powers, and Ollie's Notebook.

**Blocked by:** 08 Play a Session

**Status:** resolved

- [x] Onboarding completes in four screens and hands over to Ollie speaking the Nickname (text for now)
- [x] The disclosure line about the Nickname is present verbatim in onboarding
- [x] The Parent Gate requires a continuous press of about three seconds and cannot be opened by tapping
- [x] The Parent Area shows the Knowledge Estimate and Mastered state for all seven Skills
- [x] The browser smoke test extends through onboarding and the gate

## Comments

**2026-09-12, implementation.** The Profile gained an `identity` (`src/profile/identity.ts`: the Nickname, one of four Avatar colours, one of six Themes) and moved to version 2; a stored version 1 Profile is carried forward with its progress and no identity, so onboarding runs once and nothing is lost. Onboarding (`src/app/Onboarding.tsx`) is rendered by `/` until the identity exists and is four screens in one card built from the canvas: "What should Ollie call you?" with the only text input in the app, the Avatar colour with a live preview, the Theme from six flat pictures (`src/ui/ThemeIcon.tsx`, the canvas's own), and the note, whose line is the constant `NICKNAME_DISCLOSURE` ("The Nickname is sent to generate Stories and audio. Nothing else leaves this device. No account, no recording.") and is asserted verbatim in the smoke. Start playing writes the identity and the home screen appears with Ollie talking the greeting by Nickname (`greeting` in `src/play/lines.ts`, read-time talking as on the Session screen until ticket 11). `/play` before onboarding goes back to `/`. The Parent Gate is a Grown-ups control on the home screen leading to `/parent`, where `ParentGate` renders a lock the Parent holds: the reducer in `src/parent/gate.ts` opens only on an `elapsed` event from a press still held after `PARENT_GATE_HOLD_MS` (3000); the button has no click handler, pointer capture keeps a drifting finger on it, and the ring fills over the hold. The gate is React state on the route, so reloading or visiting `/parent` directly finds it closed again. The Parent Area (`ParentArea.tsx`) shows the Nickname and Sessions played, Mastery per Skill from `masteryRows` in `src/parent/mastery.ts` (all seven Skills under their Units, the Knowledge Estimate as a percent and bar, state Mastered / in progress / not started; Unit 3's two Skills are named there with no Estimate until ticket 10), and placeholders with their eventual content for the Parent Summaries, Ollie's Notebook, and Powers. Unit names now come from one Loop export, `unitName`.

Two ticket 08 fixes fell out of building on it: the paper-button shadow tokens are now declared on `.paper-button` as well as `:root`, because a custom property that references another is resolved where it is declared, so `--button-shadow-color` set on a key or a sky button had never been picked up (every button showed a sun-deep shadow); and the Playwright server now copies `public/` into the standalone output as the Dockerfile does, so the Path stop's Ollie head loads in the smoke. `BigButton` gained a `tone` (sun or paper) for the Back button. The smoke's second miss is now a number that is never the answer (twice "one less" was the answer for 1).

Tests: `src/profile/profile.test.ts` (identity round trip, version 1 migration, unknown identities rejected, the four colours and six Themes, Nickname cleaning), `src/parent/gate.test.ts` (a tap never opens; the hold opens at 3000 ms; a timer after release does not; short presses do not add up), `src/parent/mastery.test.ts` (seven rows, Estimates, in progress after a first attempt, Mastered), and `e2e/session.spec.ts` extended through onboarding and the gate (tap, tap, hold one second: closed; hold three: open; the seven rows with the Estimate the Profile holds; leaving finds the gate closed; still no request leaves the host), with `e2e/onboarding.ts` as the shared set-up for the other browser tests. Verified with `pnpm typecheck`, `pnpm lint`, `pnpm test` (256), `pnpm test:e2e` (14). No new dependencies.

Carried forward: ticket 10 gives each Theme its vocabulary and reads `identity.theme` and `identity.nickname` for Stories; ticket 12 fills the Parent Summaries and Notebook placeholders; ticket 13 the Powers list; ticket 14 changes the Theme in the Shop and dresses the Avatar.
